"use strict";
let minhaLista = [1, 2, 3];
function pegaPrimeiroElemento(lista) {
    return lista[0];
}
pegaPrimeiroElemento(minhaLista);
pegaPrimeiroElemento(['a', 'b']);
//nao precisa passar ele é esperto para descobrir sozinho
pegaPrimeiroElemento(minhaLista);
